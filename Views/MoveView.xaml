<UserControl x:Class="ProjectSky.Views.MoveView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:ProjectSky.Views" mc:Ignorable="d">
 <Grid Background="#252525" Margin="8">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <DockPanel Margin="0,0,0,8">
   <TextBlock DockPanel.Dock="Right" Text="Select a move to edit its complete battle definition" Foreground="#AAA" VerticalAlignment="Center" Margin="10,0,0,0"/>
   <ComboBox ItemsSource="{Binding MvDevID.moves}" DisplayMemberPath="name" SelectedValuePath="devName" SelectedItem="{Binding Selected, Mode=TwoWay}" Width="300" Padding="6" Background="#202020" Foreground="White"/>
  </DockPanel>
  <Grid Grid.Row="1">
   <Grid.ColumnDefinitions><ColumnDefinition Width="260"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
   <Border Background="#202020" CornerRadius="5" Margin="0,0,8,0" Padding="6">
    <ListBox ItemsSource="{Binding MvDevID.moves}" SelectedItem="{Binding Selected, Mode=TwoWay}" Background="#202020" Foreground="White" BorderThickness="0" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.IsDeferredScrollingEnabled="True">
     <ListBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding name}" Padding="7"/></DataTemplate></ListBox.ItemTemplate>
    </ListBox>
   </Border>
   <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
    <Grid Margin="5">
     <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <StackPanel Grid.Column="0" Margin="5">
      <TextBlock Text="MOVE DEFINITION" FontSize="20" Foreground="White" Margin="0,0,0,8"/>
      <TextBlock Text="Move ID" Foreground="#BBB"/><TextBox Text="{Binding SelectedMove.Data.move_id, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Type" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.type, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Category" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.category, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Power" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.power, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Accuracy" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.accuracy, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="PP" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.pp, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Priority" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.priority, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Effect Sequence" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.effect_sequence, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Target" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.raw_target, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Hits (Min / Max)" Foreground="#BBB" Margin="0,8,0,0"/><StackPanel Orientation="Horizontal"><TextBox Width="80" Text="{Binding SelectedMove.Data.hit_min, UpdateSourceTrigger=PropertyChanged}"/><TextBox Width="80" Margin="6,0,0,0" Text="{Binding SelectedMove.Data.hit_max, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
      <TextBlock Text="Crit Stage" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.crit_stage, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Flinch / Recoil" Foreground="#BBB" Margin="0,8,0,0"/><StackPanel Orientation="Horizontal"><TextBox Width="80" Text="{Binding SelectedMove.Data.flinch, UpdateSourceTrigger=PropertyChanged}"/><TextBox Width="80" Margin="6,0,0,0" Text="{Binding SelectedMove.Data.recoil, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
     </StackPanel>
     <StackPanel Grid.Column="1" Margin="5">
      <TextBlock Text="FLAGS &amp; BEHAVIOR" FontSize="20" Foreground="White" Margin="0,0,0,8"/>
      <TextBlock Text="Affinity" Foreground="#BBB"/><TextBox Text="{Binding SelectedMove.Data.affinity, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Quality" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.quality, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Status / Effect Value" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.inflict.value, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Effect Chance" Foreground="#BBB" Margin="0,8,0,0"/><TextBox Text="{Binding SelectedMove.Data.inflict.chance, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Text="Stat Effect 1 / Stage" Foreground="#BBB" Margin="0,8,0,0"/><StackPanel Orientation="Horizontal"><TextBox Width="80" Text="{Binding SelectedMove.Data.stat_amps.fstat1, UpdateSourceTrigger=PropertyChanged}"/><TextBox Width="80" Margin="6,0,0,0" Text="{Binding SelectedMove.Data.stat_amps.fstat1_stage, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
      <TextBlock Text="Stat Effect 2 / Stage" Foreground="#BBB" Margin="0,8,0,0"/><StackPanel Orientation="Horizontal"><TextBox Width="80" Text="{Binding SelectedMove.Data.stat_amps.fstat2, UpdateSourceTrigger=PropertyChanged}"/><TextBox Width="80" Margin="6,0,0,0" Text="{Binding SelectedMove.Data.stat_amps.fstat2_state, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
      <TextBlock Text="FLAGS" FontSize="20" Foreground="White" Margin="0,18,0,6"/>
      <ItemsControl ItemsSource="{Binding FlagComboBoxItems}"><ItemsControl.ItemTemplate><DataTemplate><CheckBox Content="{Binding Value}" IsChecked="{Binding IsChecked, Mode=TwoWay}" Command="{Binding DataContext.ToggleCheckBoxCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Foreground="White" Margin="0,2"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
     </StackPanel>
    </Grid>
   </ScrollViewer>
  </Grid>
  <TextBlock Grid.Row="2" Text="Edit fields directly; Save remains available from the Project Sky menu. Move data is virtualized to keep large move lists responsive." Foreground="#AAA" Margin="4,8,0,0"/>
 </Grid>
</UserControl>